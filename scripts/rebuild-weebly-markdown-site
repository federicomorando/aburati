#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "$0")/.." && pwd)"
cd "$repo_root"

./scripts/extract-weebly-to-hugo-md.py
hugo --source hugo-weebly-md --minify

echo "Rebuilt Markdown-based Hugo site in hugo-weebly-md/public"
